# M13: Multi-Provider Telephony Resource Management -- Progress

## Task Status

| Task | Title | Status | Completed |
|------|-------|--------|-----------|
| T01 | Telephony domain model + migration envelope | Done | 2026-04-03 |
| T02 | Provider account control plane + secret references | Done | 2026-04-03 |
| T03 | Provider-pack contract + capability matrix (Telnyx, Genesys) | Done | 2026-04-03 |
| T04 | Trunk and route inventory APIs + reconciliation | Done | 2026-04-03 |
| T05 | Number inventory and acquisition/import APIs | Done | 2026-04-04 |
| T06 | Governed number-binding refactor + DID invariant preservation | Done | 2026-04-04 |
| T07 | Deployment-default telephony and tenant BYO policy model | Done | 2026-04-04 |
| T08 | Minimal telephony workspace: providers + numbers | Done | 2026-04-04 |
| T09 | Assistant attach flow + progressive disclosure guardrails | Done | 2026-04-04 |
| T10 | Verification, runbooks, and proof harness for multi-provider telephony | Not Started | |
| T11 | Local real-call profile + inbound/outbound PSTN proof | In Progress | |
| T12 | Telnyx carrier events in observability evidence rail | Done | 2026-04-07 |
| T13 | Runtime settings centralization follow-on | Parked future note | |
| T14 | Shared routing readiness contract | Not Started | |
| T15 | Derived readiness reader convergence | Not Started | |
| T16 | Binding model derived-status cleanup | Not Started | |
| T17 | Sync-owned trunk lifecycle | Not Started | |
| T18 | Operator telephony workspace simplification | Not Started | |
| T19 | Internal telephony automation and diagnostics boundary | Not Started | |
| T20 | Realtime turn provider retry + Grove tool activity boundary | Done | 2026-04-15 |

## Notes

Milestone activated on 2026-04-03 by explicit human instruction.

M13 acceptance remains open until T11 proves a local-real-call profile that can receive and place real PSTN calls from a developer laptop using the new telephony control plane. T01-T09 delivered the control-plane substrate; T11 closes the last-mile product truth. The latest hardening step is a new `tools/scripts/telephony/real_call_precheck.py` preflight that catches the exact runtime-drift class that caused the later silent call: it validates the active cluster, host-level LiveKit/SIP reachability, the operator test-call route, speech-provider credentials/ADC inside `agent-worker`, Telnyx provider connectivity, active LiveKit-bound trunks, and optional number routing readiness before a human dials a real phone. The first live run already proved its value on `k3d-grove-local`: all runtime and telephony inventory checks passed, but the script failed fast on missing Vertex/Google ADC in `agent-worker`, which is exactly the silent-call failure mode we previously discovered only after the handset answered.
The next fresh-cluster rehearsal on `main` exposed three operational gaps that are now documented in the runbooks and T11 notes: `platform-web` can still hide `Developer access` on `/admin-login` unless `GROVE_ENABLE_TOKEN_PASTE_LOGIN=true` is set live; `tools/scripts/telephony/reconcile_bootstrap.py` still stops after provider-account + trunk reconciliation and does not automatically run deployment `/numbers/sync`; and a fresh DB can still have zero tenants, zero agent definitions, and zero `phone_number_bindings`, so inbound DID lookup remains fail-closed even after the Telnyx DID has been imported into `public.telephony_numbers`.
The same rehearsal also made the Google runtime contract concrete instead of implicit. The old local hotfix mixed a `manibo-production` service-account key with `GOOGLE_CLOUD_PROJECT=nfq-voice-staging`, which is exactly how answered-but-silent calls turn into debugging theater. The new recorded rule is: one deployment -> one Google project -> one auth source -> one region policy. The explicit environment matrix is now written into T13 and the runbooks: NFQ production uses `call-platform-production`, NFQ staging uses `basic-buttress-489414-i1`, Manibo production uses `manibo-production`, and local k3d currently defaults to NFQ production for the local-real-call proof. Clusters must not accept `authorized_user` ADC in `gcp-adc`; local and Hetzner use mounted service-account JSON, while GKE uses Workload Identity.
The client-tested Lithuanian voice demo has now been ingested and compared against the platform clinic agent. The adopted baseline is Soniox `stt-rt-v4` with strict `lt` hints and structured context, Vertex/Gemini `gemini-2.5-flash`, Google Chirp3-HD `lt-LT-Chirp3-HD-Aoede` in `europe-west2`, `preemptive_generation`, `resume_false_interruption`, `max_tool_steps=10`, and `user_away_timeout_s=15.0`. The prompt update is intentionally narrow: preserve the existing STT/TTS voice-call framing and add the tested spoken-call rules rather than copying the demo's full multi-agent Affidea prompt. The demo prompt files are now mapped in `wiki/ops/voice-call-local-demo.md`: global voice rules and static STT keywords are adopted, consultation/auth prompts are only partially compatible with current platform tools, and the MRI prompt is explicitly blocked until there is a production MRI booking/safety/preparation contract. The platform still does not implement the demo's dynamic post-lookup Soniox context mutation with patient name parts; that is now recorded as a runtime follow-up, not prompt text.
The browser test workbench WebSocket regression is now documented and covered. The symptom was `could not establish signal connection` with LiveKit returning `404` for a valid `/rtc` handshake. The root cause was the local LiveKit ingress forcing `Connection: keep-alive`, which stripped the WebSocket upgrade path. The fix removes that ingress annotation, adds manifest coverage that WebSocket upgrade headers stay preserved, and adds cluster-backed E2E coverage that draft and published browser-session tokens can establish the API-returned LiveKit signal connection while the operator token/takeover path still works.
The next workbench proof closed a different gap: signal connectivity was not enough to prove a usable browser voice session. The failing local stack created rooms and dispatched the agent, but the browser participant never completed ICE because the `k3d` server load balancer did not publish LiveKit Server's `7881/TCP` RTC mux. The startup script now publishes that port for voice-enabled local clusters, reports it in the local URL output, and the browser-session E2E now uses LiveKit SDK `Room.connect` plus agent-participant detection instead of the old websocket-only check.
GCP production is no longer app-only for voice. The production overlay now deploys `agent-worker` with Workload Identity, the GAR `agent-worker` image pin, scoped runtime secrets, and no JSON ADC mount. The local LiveKit ingress is no longer part of shared `base/platform/core`; local/CI and Hetzner self-hosted paths include it explicitly, while GCP cloud mode renders no local LiveKit ingress and relies on the LiveKit Cloud URLs from scoped production secrets.

The 2026-04-15 local browser voice proof changed the current blocker again. Transport and speech providers are no longer the primary issue for the published `clinic_registrator` v2 path: the browser can connect, stream audio, receive STT/TTS, call Vertex/Gemini, and execute `search_clinic_booking_options`. The open bugs are now product/runtime correctness bugs: the structured flow repeats `specialty selection` and forgets or fails to combine city plus specialty across turns, completed browser sessions can remain shown as `in progress` in the Runs list, historical run replay can show an empty Conversation tab plus an unhelpful Inspector despite a visible trace tree, and the browser-session E2E needs to assert a real assistant transcript instead of only proving LiveKit room join. These are now recorded in T11 so they survive context compaction.
T20 now captures the runtime-boundary follow-up from the same browser/phone-call debugging session. The settled ownership rule is: Grove owns the generic tool execution contract and Temporal workflow/activity definitions; `grove-voice-livekit` uses that contract from the realtime voice worker; `apps/temporal-worker` only hosts/registers the Grove workflow and activity for the deployment. LLM streaming stays in-process with short retry/backoff for retryable provider throttles such as Vertex `429` / `RESOURCE_EXHAUSTED`, while tool calls cross a Grove-owned Temporal activity boundary. Tests for this work must cover production Grove/voice/worker code paths only, not helper scripts or local bootstrap utilities.
T20 is implemented. `packages/grove/src/grove/runtime/llm_calls.py` now retries retryable provider throttles before any model-output chunk reaches TTS, records retry/exhaustion logs, spans, and counters, and refuses to replay a stream after text or tool-call output has started. `AgentExecutor` now routes tool calls through an injected backend while preserving the in-process backend. Grove owns the generic `grove.ToolExecutionWorkflow` and `grove.execute_tool` activity contract, including tool result, tool-call record, error envelope, and captured runtime events. `grove-voice-livekit` starts the Grove tool workflow for realtime voice tool calls when Temporal is enabled, then replays returned runtime events into the existing live-call evidence path. `apps/temporal-worker` only registers and hosts the Grove workflow/activity with the deployment's composed voice tool registry. Focused proof covered Grove retry behavior, the executor backend seam, the real Temporal workflow/activity path, voice backend workflow dispatch and event replay, worker registration, Temporal naming, and Grove import boundaries.
The operator console live-call controls have been simplified after the real inbound phone-call proof. `/call-ops` no longer hides the transcript behind a separate "Watch transcript" action, no longer exposes a broken support/details drawer, and no longer presents "Join call" or "Transfer" as separate concepts. The product contract is now explicit: `Listen` joins the LiveKit room with subscribe-only audio and microphone disabled; `Take over` joins with an operator-publish token, starts browser audio, enables the microphone, and only then signals manual takeover so the AI agent is removed after the browser is already present. The cluster-backed E2E now exercises the same safe ordering with a real LiveKit operator token and confirms the operator participant remains connected after the agent is removed.

T12 is now planned as the observability follow-on to the live PSTN debugging pain discovered in T11. The key lesson is that the platform still goes blind when a carrier-side failure happens before `livekit-sip` or `platform-api` emits anything, even though Telnyx still knows whether it accepted the DID, which SIP connection/FQDN it tried, whether `180/183` or `200 OK` ever arrived, and what upstream SIP or hangup cause ended the attempt. T12 closes that blind spot by landing Telnyx FQDN connection events into the existing M1 evidence-rail/live-streaming surfaces instead of building a separate telephony debug UI. The target architecture is: Telnyx webhook ingress -> verification/dedup -> correlation by `connection_id` + DID + provider call IDs -> persistence into the same call runtime evidence stream -> `/calls/{id}/ops/stream` + observability timeline/detail APIs -> M1 evidence rail.
T12 is now started with the first stable backend seam: `packages/platform-core/src/platform_core/voice/telnyx_webhook.py` adds a real platform-owned `/webhooks/telnyx/voice` ingress that verifies Telnyx Ed25519 signatures (`telnyx-timestamp` + `telnyx-signature-ed25519`) and normalizes the raw carrier payload into a platform-owned `NormalizedTelnyxCarrierEvent`. That keeps the next slice honest: correlation and persistence should extend one normalized model into `call_runtime_events` and the M1 evidence rail, instead of spreading raw Telnyx JSON parsing through API routes and UI components.
T12 is complete. `packages/platform-core/src/platform_core/voice/telnyx_carrier_events.py` now verifies webhook freshness, fails closed when correlation is weak, deduplicates on persisted `carrier_event_id`, correlates by `connection_id` + bound DID + provider call identifiers, and writes normalized carrier facts into `call_runtime_events` plus `calls.metadata["carrier"]`. `apps/temporal-worker/src/temporal_worker/voice_activities.py` now seeds a best-effort outbound `calls` row before SIP dialing so later Telnyx events can attach to a real call/session instead of racing the first persisted platform event. No new SSE or UI pipeline was needed: `/calls/{id}/ops/stream` already streams `call_runtime_events`, and `apps/api/src/platform_api/routes/observability.py` only needed to classify `carrier.*` events as `route` so the existing M1 evidence rail and timeline surfaces render them generically. The latest inbound-debugging lesson is now encoded directly into that rail contract: `486 flood` from self-hosted `livekit-sip` is not a generic transport error, it is the pre-dispatch `AuthDrop` branch, so the platform now records it as a first-class carrier-route failure with explicit diagnostics instead of burying it as a raw SIP response code.

T11 is now in progress. The current branch defines the local-real-call acceptance profile, adds self-hosted/public-IP SIP host handling to `tools/scripts/setup-sip.py`, and adds the first minimal operator outbound trigger through `POST /calls/test-call` in `apps/api/src/platform_api/routes/calls_live.py`. That route resolves a live bound telephony number plus its published governed agent through the telephony inventory, then starts `VoiceCallWorkflow` with the resolved LiveKit SIP trunk instead of relying only on deployment-wide env defaults. The latest PSTN debug run found the actual blocker behind one round of misleading results: the patched `platform-temporal-worker` image was imported into a different k3d cluster name than the active `kubectl` context, so the live `k3d-grove-local` cluster kept running a stale worker pod with the old `voice_activities.py` code. That cluster mismatch is now part of the recorded execution trail because it explains why the live phone call kept exercising old behavior after the code fix had already landed locally. Outbound PSTN proof is now real: operator test call `<redacted-outbound-call-id>` rang a real handset from a redacted bound source DID, the Demo Agent spoke over the self-hosted local-real-call path, and the conversation completed successfully enough to book a doctor visit. A later connected-but-silent call (`<redacted-silent-call-id>`) proved the next operational weakness: telephony was healthy, but the running `agent-worker` deployment had no usable Vertex/Google ADC runtime contract for LLM/TTS, so the human heard silence even though the SIP leg, room join, and initial greeting trigger all succeeded. That failure is now documented as a runtime-credential drift problem, not a Telnyx/LiveKit transport problem. The current recovery is codified as Vertex-only: the live `agent-worker` and `platform-temporal-worker` pods carry `GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/google/application_default_credentials.json`, `GOOGLE_CLOUD_PROJECT=call-platform-production`, and `GOOGLE_CLOUD_LOCATION=europe-central2`, with no `GEMINI_API_KEY` in `platform-runtime-secrets`. The precheck now reads the runtime pod env through `kubectl exec`, and the live preflight on `k3d-grove-local-realcall` exits clean once those credentials are wired. Inbound PSTN proof is still pending before M13 can close.
The newest inbound trace finally crossed the line from “carrier routing” into “answered local call.” `livekit-sip` matched a redacted inbound trunk, joined a redacted LiveKit room, accepted the call, and mixed the agent audio track. `platform-api` accepted `POST /webhooks/livekit/room-started`, seeded `call_id=<redacted-inbound-call-id>`, and started `InboundCallOrchestratorWorkflow`. The first remaining blocker at that point was not SIP at all: `platform-runtime-secrets` was still overriding the local configmap with a redacted LiveKit Cloud URL, so room metadata updates were being sent to the old cloud endpoint and failing until `tools/scripts/k8s-runtime-secrets.sh` was hardened to rewrite local `LIVEKIT_URL` / `LIVEKIT_API_URL` to `http://livekit-livekit-server.platform.svc.cluster.local`.
After that local runtime-secret fix, `agent-worker` stopped failing on empty metadata. The live pod logged `Voice job room metadata became available after 1005ms`, resolved `appointment_booking`, and started the voice session. The human still heard silence because Google TTS failed in the live worker with `google.auth.exceptions.DefaultCredentialsError`. The root cause was concrete: the optional `gcp-adc` volume mount existed in the deployment, but the actual `gcp-adc` secret was missing in namespace `platform`, `/var/secrets/google` was empty, and the worker env had neither `GOOGLE_APPLICATION_CREDENTIALS` nor `GOOGLE_CLOUD_PROJECT`. Creating the `gcp-adc` secret from the developer ADC file and setting those two env vars on `agent-worker` is now the proven runtime recovery path for the answered-but-silent inbound call.
Test coverage is better but still not enough to call this class closed. We now have a regression test that local `tools/scripts/k8s-runtime-secrets.sh` rewrites `LIVEKIT_URL` / `LIVEKIT_API_URL`, plus architecture tests that the local worker image ships the clinic plugin and does not suppress the greeting. What is still missing is a deterministic local-real-call bootstrap/runtime test that proves the `gcp-adc` secret and the required Google env vars exist in `agent-worker` before a real PSTN call is placed. Until that exists, the precheck script remains the operational gate instead of a full automated proof.
The operator runbooks are now aligned with that reality. `README.md` now includes a first-call quick path, `wiki/ops/phone-number-onboarding.md` documents how to register the first provider-backed number and start an operator test call, and `wiki/ops/voice-call-local-demo.md` documents the supported debugging path for transcript and runtime inspection. The default inspection surfaces are now the call detail API plus the two SSE streams, not raw SQL: `GET /calls/{call_id}`, `GET /calls/{call_id}/transcript/stream`, and `GET /calls/{call_id}/ops/stream`.
The newest inbound debugging pass proved the remaining local gap is in the SIP advertise layer, not in the telephony inventory or Telnyx defaults. Host `5060/5061` plus RTP are now published by `k3d`, the `livekit-sip` Service exposes the right ports, and the live Telnyx FQDN connection shows the expected self-hosted primary/secondary FQDNs and `30s/60s` inbound wait windows. But `livekit-sip` startup logs still showed `external: 10.42.1.11`, because the base deployment was launching from a static config with `use_external_ip: false`. Production already worked around that with a runtime wrapper that appends `nat_1_to_1_ip` from node metadata. The branch now ports the same idea to the shared base deployment by rendering `sip_hostname` from `LIVEKIT_SIP_HOST` and, when that host is a bare IPv4, also appending `nat_1_to_1_ip` plus `media_nat_1_to_1_ip` before `livekit-sip` starts. That gives the local public-IP profile a real advertised SIP/media address instead of leaking the pod IP into carrier-facing signaling.
On 2026-04-12 the milestone gained an explicit simplification follow-on backlog (T14-T19) by direct human instruction. That follow-on does not reopen the basic multi-provider substrate. It narrows the product contract: one shared derived readiness rule across inbound + observability + UI, no operator-facing trunk archive/reconcile lifecycle, provider sync as the only explicit maintenance action, and bootstrap/LiveKit/trunk repair moved behind internal automation or diagnostics-only seams.
The next hardening step turns local cluster startup into the source of truth instead of another manual runbook trap. `tools/scripts/telephony/reconcile_bootstrap.py` now runs during local voice-enabled `k3d` runtime sync, seeds `infrastructure/kubernetes/overlays/local/default/secrets.env` from the main worktree `.env` or the current cluster secret when a feature worktree starts without ignored secret files, provisions Telnyx + LiveKit SIP idempotently, and reapplies `platform-runtime-secrets` with the generated IDs. The first live reconcile also corrected an architectural assumption in the earlier T11 notes: the working local profile is not using LiveKit Cloud and it is not provisioning against a host-level LiveKit API endpoint. The actual successful path is in-cluster LiveKit Server accessed through a temporary `kubectl port-forward` to `svc/livekit-livekit-server`, combined with the configured public SIP host for the carrier-facing Telnyx FQDN record. That run created a redacted Telnyx FQDN record, confirmed the existing inbound trunk, reasserted the outbound trunk, and persisted `LIVEKIT_SIP_INBOUND_TRUNK_ID`, `LIVEKIT_SIP_OUTBOUND_TRUNK_ID`, `LIVEKIT_SIP_DISPATCH_RULE_ID`, `TELNYX_CONNECTION_ID`, and the self-hosted SIP host back into the local runtime secret source so future `k3d-up.sh` runs stop booting with placeholder inbound values.
The next live finding was outside the cluster: the active Telnyx FQDN connection still routed inbound calls to old LiveKit Cloud and `localtest.me` records by default, even though the self-hosted laptop public-IP records already existed on the same connection. That explained why outbound proof worked while inbound PSTN still died before the platform. The live connection has now been hardened to the intended self-hosted shape: redacted primary and secondary FQDNs, no tertiary fallback, `timeout_1xx_secs=30`, `timeout_2xx_secs=60`, `sip_compact_headers_enabled=false`, preferred codecs `G711U`/`G711A`, with `transport_protocol=TCP` and `encrypted_media=SRTP` preserved. More importantly, the code now owns that state too: `tools/scripts/setup-sip.py --self-hosted-livekit` applies the same Telnyx inbound defaults, so both local and deployed self-hosted clusters can bootstrap telephony from code instead of Mission Control drift.
The next inbound retry still failed, but this time the evidence was clean: there were zero fresh logs in `platform-api`, `platform-temporal-worker`, `agent-worker`, or `livekit-sip`. That ruled out DID lookup, webhook handling, and agent speech entirely. The actual blocker was the local `k3d` network shape. `docker ps` for `k3d-grove-local-serverlb` showed only host ports `80` and `443`; no `5060`, no `5061`, and no RTP UDP range were published on the laptop host. At the same time, the in-cluster `livekit-sip` Service exposed `5061/TCP` plus RTP UDP ports, but not `5060/TCP`, while the live Telnyx FQDN connection correctly targeted the configured public SIP host on `5060/TCP`. So outbound proof still does not imply inbound readiness: outbound calls originate from inside the cluster and never need the laptop host to accept public SIP, whereas inbound PSTN needs both the `k3d` server load balancer and the `livekit-sip` Service to publish the actual signaling/media ports. That fix is now real in code and runtime: `k3d-up.sh` publishes `5060/TCP`, `5060/UDP`, `5061/TCP`, and `10000-10019/UDP` by default for voice-enabled local clusters, and `service-livekit-sip.yaml` exposes `5060/TCP` so the self-hosted TCP path that `setup-sip.py` provisions can actually receive real carrier traffic.
The latest deep-debug pass moved the remaining fault fully out of the cluster. A raw SIP `INVITE` to `127.0.0.1:5060` now reaches `livekit-sip`, matches the configured inbound trunk plus dispatch rule, and logs a clean local `488 no-common-crypto`, which proves the k3d-hosted SIP edge itself is functioning. The same probe to the configured public SIP host fails with `ECONNREFUSED`, and the host interface inventory shows the active laptop interface behind a private LAN address while the route to the public SIP host goes through the router gateway. In other words: the laptop is still behind NAT, and the public SIP host is the router's public address, not a local interface. Real inbound PSTN will not reach the local stack until the upstream router forwards `5060/TCP`, `5060/UDP`, `5061/TCP`, and `10000-10019/UDP` to the laptop (and ideally disables SIP ALG), or until the SIP edge moves to a host with a true public interface.
Finishing that fix surfaced two more operational traps that are now codified. First, the old host-level manual `livekit-sip` process from an earlier debug session was still bound to `5060/TCP` and `5060/UDP`, so `k3d` could not publish the rebuilt cluster’s SIP ports until the stale host process was stopped. Second, once the cluster came back, `platform-runtime-secrets` was still leaking `TEMPORAL_ADDRESS=localhost:7234` from the gitignored local `secrets.env`, which overrode the correct in-cluster value from `platform-runtime-config` and crashed `platform-api` plus `platform-temporal-worker` on Temporal connect. `tools/scripts/k8s-runtime-secrets.sh` now rewrites `TEMPORAL_ADDRESS` to `temporal-frontend.platform.svc.cluster.local:7233` for local overlay cluster rendering, so rebuilt clusters converge on the cluster-safe address instead of inheriting host-only port-forward settings. After that fix, `tools/scripts/k3d.sh up` completed successfully, `curl http://api.grove.localtest.me/health` returned `{"status":"ok"}`, and `platform-api`, `platform-temporal-worker`, `agent-worker`, `platform-web`, plus `livekit-sip` all came back healthy on the rebuilt cluster.
Once the Omada router forwarded the full SIP/RTP set to the laptop, inbound finally reached the local platform stack. `platform-api` accepted real `room-started` webhooks, `platform-temporal-worker` started `InboundCallOrchestratorWorkflow`, and the failure moved from carrier/network blind spots into the local voice runtime. The first cluster-side bug was a race in `packages/grove-voice-livekit/src/grove_voice_livekit/voice_job.py`: LiveKit could dispatch the worker before the platform finished writing room metadata, so the worker read `ctx.room.metadata=""` and disconnected immediately. That path is now hardened with a short metadata wait plus explicit logs for waiting, success, and timeout failure, and the regression coverage for both cases now lives in `packages/grove-voice-livekit/tests/test_entrypoint.py`.
The next live inbound trace exposed two more local-runtime issues that looked like telephony failures from the handset side. First, the local `agent-worker` deployment still hard-coded `GROVE_VOICE_DISABLE_INITIAL_GREETING=1`, so even a healthy inbound call would sit silent until the caller spoke first. That env support is now removed entirely from the worker code path, the base k8s deployment, Compose/dev helper scripts, and the related observability e2e harness env. Second, the live worker image could not import `appointment_booking` because `apps/agent-worker/Dockerfile` never copied or installed `solutions/appointment_booking`. The Dockerfile now includes that package, and a dedicated architecture test locks the requirement in so the clinic plugin cannot silently disappear from future worker images.

The same telephony debugging work surfaced one follow-on feature request that is real but explicitly parked: runtime settings are too fragmented. T11/T12 had to chase missing or drifting values across `platform-api`, `platform-temporal-worker`, `agent-worker`, local overlay secrets, and bootstrap scripts. That is exactly the kind of configuration drift a single typed Pydantic Settings contract should remove, but it is now tracked as T13 so inbound/outbound PSTN proof stays the active milestone focus instead of turning M13 into a repo-wide config refactor.

T01 established the typed telephony resource model, added public-schema tables for provider accounts/trunks/numbers/bindings, and locked the initial scope to a clean-slate telephony setup without a legacy `public.phone_numbers` bridge.

T02 added the provider-account control plane in `packages/platform-core/src/platform_core/telephony/provider_accounts.py` and `apps/api/src/platform_api/routes/telephony_provider_accounts.py`, including deployment-scoped vs tenant-scoped ownership, audited lifecycle transitions, and a provider-agnostic connectivity-test contract composed with a Telnyx balance probe in the API layer. The current in-repo browser workspace intentionally consumes only the deployment-admin slice through `/admin/telephony/provider-accounts`; tenant BYO control-plane APIs remain mounted under `/telephony/provider-accounts` plus `/admin/tenants/{tenant_id}/telephony/provider-accounts/{provider_account_id}` for future tenant surfaces and server-side provisioning.

T03 added the Layer-3 telephony provider-pack contract, entry-point discovery registry, and concrete Telnyx/Genesys provider-pack scaffolds under `solutions/provider_telnyx` and `solutions/provider_genesys`. Provider-account capability snapshots now come from installed provider packs, the Telnyx connectivity probe moved out of `apps/api` into the Telnyx pack where it belongs, and `/admin/telephony/provider-options` now publishes the deployment-composed provider list so create flows stop advertising providers that are not installed here. The Genesys pack now encodes the real first-pass gap honestly: import-only sync metadata with no browser metadata editor and no in-product number acquisition exposed yet.

T04 added persisted trunk inventory and reconciliation through `packages/platform-core/src/platform_core/telephony/trunks.py` and `apps/api/src/platform_api/routes/telephony_trunks.py`. Telnyx trunk sync now imports credential and FQDN connections from the documented Telnyx inventory endpoints, Genesys import-only trunk sync reads structured `provider_metadata.trunks`, and the mounted control-plane API covers tenant plus deployment list/create/update/sync/reconcile/archive flows with admin-tenant LiveKit binding provisioning for BYO trunks. Trunk drift is tracked against both provider resource IDs and LiveKit binding IDs so operators can stop treating hidden trunk env vars as the control plane. The current `/admin/telephony` workspace intentionally consumes the deployment-admin inventory/sync slice only; trunk create/update/archive/reconcile and tenant BYO flows remain API-only in M13.

T05 added persisted telephony number inventory and capability-gated acquisition/import flows through `packages/platform-core/src/platform_core/telephony/numbers.py` and `apps/api/src/platform_api/routes/telephony_numbers.py`. Telnyx now supports available-number search, phone-number inventory sync, and number-order acquisition through the provider pack; Genesys number inventory sync reads structured `provider_metadata.numbers` and keeps search/order unsupported instead of faking parity. The mounted control-plane API exposes tenant and deployment list/search/sync/acquire surfaces, while the current `/admin/telephony` workspace intentionally consumes the deployment-admin list and sync slice only. Tenant BYO number search/acquire and the deeper admin purchase flows remain API-only in M13. Synced numbers converge into one local inventory model with control-plane state instead of separate “imported vs purchased” storage.

T06 moved the admin phone-channel surface and DID lookup path onto `public.phone_number_bindings` joined to `public.telephony_numbers` and `public.telephony_trunks`. The admin contract is now `/admin/tenants/{tenant_id}/phone-channels`, and the runtime model is strict: reads and writes go through governed telephony inventory only. Active runtime/admin behavior no longer promotes or depends on legacy `public.phone_numbers` rows, so operator pages, observability reads, and DID lookup all stay read-only against the governed control plane. When operators attach a DID to an already-persisted LiveKit trunk and provider sync/order has not created a number row yet, the backend still materializes a minimal imported `public.telephony_numbers` row on that trunk and cleans it up again when the binding is deleted or moved elsewhere. Agent retirement and inbound routing therefore fail closed on governed bindings only.

T07 added an explicit tenant telephony policy switch on `public.tenants`, a typed policy service in `packages/platform-core/src/platform_core/telephony/policy.py`, and the tenant plus deployment-admin policy routes at `/telephony/policy` and `/admin/tenants/{tenant_id}/telephony/policy`. Deployment default telephony and tenant BYO telephony now have persisted, audited coexistence rules. Tenant-owned provider-account create/provision flows fail closed when the tenant policy is `default_only`, policy changes to `default_only` are rejected while tenant-owned telephony resources still exist, policy changes to `byo_only` are rejected while deployment-managed DID bindings still exist, admin phone-number binding now enforces deployment-only vs tenant-owned target eligibility, and policy responses expose allow/deny state plus usable provider-account-path status directly instead of relying on browser env values or hidden admin assumptions.

T08 is complete on the milestone branch. The current slice has the first operator-facing telephony workspace in `apps/web/src/app/(deployment)/admin/telephony/page.tsx` with plain-language `Providers` and `Numbers` tabs, plus supporting admin components and API clients. The providers view exposes installed provider accounts, ownership, connectivity state, and safe connect/test flows without surfacing control-plane internals. The numbers view exposes deployment-managed number inventory, tenant-aware assignment detail, and row-click editing while keeping raw trunk/provider IDs out of the default flow. The deployment admin workbench and dashboard now link directly to this telephony workspace, and the responsive layout stays usable on mobile by switching the dense tables to stacked cards on narrow screens. The dedicated local PR review for T08 completed clean with `tools/scripts/run_local_pr_review.sh origin/main post_ci` returning `No blocking findings.` after closing the assistant deep-link and BYO-policy regressions.

T09 is complete on the milestone branch. The assistant detail `Channels` section now reads directly from governed telephony inventory, exposes an inline `Attach existing number` picker for unassigned deployment-managed numbers, and routes deeper repair/review work into `/admin/telephony?tab=numbers...` instead of the old phone-number editor. The assistant page no longer leaks healthy-state routing IDs or transport internals; it shows business status plus the next action only. The telephony workspace now honors explicit tab selection before falling back to the contextual `tenant_id` / `assistant_id` numbers-tab default, so `/admin/telephony?tenant_id=...&tab=providers` stays on `Providers` while assistant attach flows still land on `Numbers`. The attach flow shares the same binding write seam as the inventory editor so the two surfaces cannot drift. Assistant channels now reflect governed bindings only; stale legacy `public.phone_numbers` rows are intentionally invisible to the assistant UX. T09 also added tenant-routing fallback visibility for already attached numbers, excludes degraded-trunk numbers from the attach picker, and updated the shared Playwright harness defaults so the new telephony background fetches return empty deployment-safe data in non-telephony suites instead of polluting unrelated admin tests with 502 console noise. Web proof is now green end-to-end (`check-types`, focused Vitest, targeted UI harness, full `apps/web` UI harness with `163 passed`, Playwright MCP desktop/mobile, Chrome DevTools MCP desktop/mobile), and the dedicated local PR review completed clean with `tools/scripts/run_local_pr_review.sh origin/main post_ci` returning `No blocking findings.`

Trace anchor: `docs/milestones/M13-telephony-management.md`; `docs/tasks/M13/T02-provider-account-control-plane.md`; `docs/tasks/M13/T03-provider-pack-contract.md`; `docs/tasks/M13/T04-trunk-inventory-sync-api.md`; `docs/tasks/M13/T05-number-inventory-provisioning-api.md`; `docs/tasks/M13/T06-routing-binding-governance.md`; `docs/tasks/M13/T07-default-vs-byo-policy-model.md`; `docs/tasks/M13/T08-admin-ui-provider-accounts-trunks.md`; `docs/tasks/M13/T09-admin-ui-number-inventory-assignment.md`.

## Scratchpad

- Objective: add the assistant-side attach flow without turning assistant pages into a second telephony control-plane surface.
- Assumptions: T09 should reuse the existing telephony inventory plus number-binding write seam, keep the assistant page summary-first, and preserve the deployment-admin telephony workspace as the deeper repair/inventory surface.
- Decisions: replace the assistant-side read model with telephony inventory instead of the old `/admin/channels` phone-number list, add one modal picker for unassigned deployment-managed numbers, hide healthy routing internals entirely on the assistant page, and route contextual deep work into `/admin/telephony?tab=numbers&tenant_id=...&assistant_id=...`.
- Actions: created `admin-telephony-number-picker.tsx`, rewired `channels-panel.tsx` to telephony inventory plus tenant policy checks, updated the assistant detail page to pass assistant identity into the picker, made the telephony workspace open directly on the `Numbers` tab when assistant context is present while still honoring explicit `tab=providers`, added focused Vitest coverage, updated the assistant-channel Playwright spec, updated the shared Playwright harness defaults for the new background telephony fetches, ran the targeted and full UI harnesses, and captured desktop/mobile proof in both browser toolchains.
- Evidence: `apps/web/src/app/(deployment)/admin/agent-definitions/[id]/channels-panel.tsx`; `apps/web/src/app/(deployment)/admin/agent-definitions/[id]/page.tsx`; `apps/web/src/app/(deployment)/admin/telephony/page.tsx`; `apps/web/src/components/admin-telephony-number-picker.tsx`; `apps/web/tests/channels-panel.test.tsx`; `apps/web/tests/admin-telephony-page.test.tsx`; `apps/web/e2e/admin-agent-channels.spec.ts`; `apps/web/e2e/harness.ts`; `apps/api/tests/integration/test_phone_numbers_api.py`; `apps/api/tests/integration/test_phone_numbers_api_mutations.py`; `packages/platform-core/tests/integration/test_telephony_numbers_service.py`; `tools/agents/artifacts/manual-ui/t09-playwright-desktop-assistant-channels.png`; `tools/agents/artifacts/manual-ui/t09-playwright-mobile-assistant-channels.png`; `tools/agents/artifacts/manual-ui/t09-devtools-desktop-assistant-channels.png`; `tools/agents/artifacts/manual-ui/t09-devtools-mobile-assistant-channels.png`; `tools/agents/artifacts/ui-harness/local-20260404T164550Z`; `tools/agents/artifacts/ui-harness/local-20260404T170313Z`; `tools/scripts/run_local_pr_review.sh origin/main post_ci`.
- Risks: no T09-specific blocker remains; T10 now owns milestone-wide runbook and operational proof closure rather than compensating for a missing web verification result.
- Next steps: start T10 verification, runbooks, and proof harness for multi-provider telephony.
