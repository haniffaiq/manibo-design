export enum OidcProvider {
  Default = "default",
  Google = "google",
  Microsoft = "microsoft",
}

export enum PlatformTokenPreference {
  Auto = "auto",
  IdToken = "id_token",
  AccessToken = "access_token",
}
